import React from "react";

interface Props {}

const footer = (props: Props) => {
  return (
    <footer className="h-10 bg-blue-50">
      <div>푸터</div>
    </footer>
  );
};

export default footer;
