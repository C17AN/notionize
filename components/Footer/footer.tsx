import React from "react";

interface Props {}

const footer = (props: Props) => {
  return (
    <footer className="h-32 bg-blue-50 bottom-0 right-0 left-0">
      <div>푸터</div>
    </footer>
  );
};

export default footer;
